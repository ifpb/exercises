---
title: Estilo da Página do Ping
preview: true
subjects:
  - web
areas:
  - estilo
---

import { Image } from '@astrojs/image/components';
import layout from './_assets/layout.png';

## Descrição

O comando `ping` é um utilitário de rede que permite verificar se um dispositivo está conectado à rede. O comando `ping` envia pacotes de dados para um dispositivo e aguarda a resposta. Se o dispositivo responder, o comando `ping` exibe uma mensagem informando que o dispositivo está conectado à rede. Caso contrário, o comando `ping` exibe uma mensagem informando que o dispositivo não está conectado à rede.

Para auxiliar a utilização do comando `ping`, crie uma página Web estlizada que descreva os detalhes e parâmetros do comando. A página deve ser criada com HTML e CSS e deve seguir o layout apresentado na _Figura 1_.

_Figura 1 - Página do Comando Ping Estilizado._

<Image
  src={layout}
  alt="Página do Comando Ping Estilizado"
  class="border p-4"
/>
